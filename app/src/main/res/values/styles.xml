<resources>
    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.MaterialComponents.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <style name="titleTheme" parent="AppTheme">
        <!-- Customize your theme here. -->
        <item name="android:textColor">@color/black</item>
        <item name="android:textSize">24dp</item>
        <item name="android:gravity">center</item>
    </style>

    <style name="mainvalueTheme." parent="AppTheme">
        <!-- Customize your theme here. -->
        <item name="android:textColor">@color/white</item>
        <item name="android:textSize">22dp</item>
        <item name="android:gravity">center</item>
    </style>

    <style name="recordvalueTheme." parent="AppTheme">

    <!-- Customize your theme here. -->
        <item name="android:textColor">@color/gray</item>
        <item name="android:textSize">20dp</item>
        <item name="android:gravity">center</item>
    </style>

    <style name="cardtitleTheme." parent="titleTheme">
        <item name="android:textSize">16dp</item>
        <item name="android:layout_marginLeft">5dp</item>

    </style>
    <style name="cardvalueTheme." parent="recordvalueTheme.">
        <item name="android:textSize">16dp</item>
        <item name="android:layout_marginLeft">2dp</item>
    </style>
</resources>